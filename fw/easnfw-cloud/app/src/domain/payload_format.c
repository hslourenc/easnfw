#include <stddef.h>

int payload_format_encode(const void *record, size_t record_size,
	void *payload, size_t payload_size)
{
	/* EASNFW_TODO: Define and encode the cloud platform payload schema. */
	(void)record; (void)record_size; (void)payload; (void)payload_size;
	return 0;
}